# Migration `20201103185825-day2`

This migration has been generated by Jhonny Michel at 11/3/2020, 3:58:25 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Day" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "date" DATETIME NOT NULL,
    "cardioType" TEXT,
    "cardioCount" INTEGER NOT NULL DEFAULT 0,
    "foodCalories" INTEGER NOT NULL DEFAULT 0,
    "strengthDone" BOOLEAN NOT NULL DEFAULT false,
    "strengthType" TEXT,
    "userId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Day" ("id", "createdAt", "updatedAt", "date", "userId") SELECT "id", "createdAt", "updatedAt", "date", "userId" FROM "Day";
DROP TABLE "Day";
ALTER TABLE "new_Day" RENAME TO "Day";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201101193759-days..20201103185825-day2
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -41,12 +41,13 @@
   id                 Int       @default(autoincrement()) @id
   createdAt          DateTime  @default(now())
   updatedAt          DateTime  @updatedAt
   date               DateTime
-  cardio_type        String
-  cardio_done        Boolean   @default(false)
-  food_calories      Int       @default(0)
-  strength_done      Boolean   @default(false)
+  cardioType        String? // activeCalories | steps
+  cardioCount       Int       @default(0)
+  foodCalories      Int       @default(0)
+  strengthDone      Boolean   @default(false)
+  strengthType      String?
   user               User?     @relation(fields: [userId], references: [id])
   userId             Int?
 }
```


